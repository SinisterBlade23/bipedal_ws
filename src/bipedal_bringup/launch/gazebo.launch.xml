<launch>
    <let name="urdf_path" 
         value="$(find-pkg-share bipedal_description)/urdf/bipedal_description.urdf.xacro"/>
    <let name="gazebo_config_path" 
         value="$(find-pkg-share bipedal_bringup)/config/gazebo_bridge.yaml"/>
    <let name="controllers_config_path"
         value="$(find-pkg-share bipedal_bringup)/config/ros2_controllers.yaml"/>

    <!-- Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher">
        <param name="robot_description"
               value="$(command 'xacro $(var urdf_path)')"/>
        <param name="use_sim_time" value="true"/>
    </node>

    <!-- RViz -->
    <node pkg="rviz2" exec="rviz2" output="screen"/>

    <!-- Gazebo -->
    <set_env name="GZ_SIM_RESOURCE_PATH" value="$(find-pkg-prefix bipedal_description)/share"/>
    <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
        <arg name="gz_args" value="$(find-pkg-share bipedal_bringup)/worlds/bipedal.sdf -r"/>
    </include>

    <!-- Spawn robot -->
    <node pkg="ros_gz_sim" exec="create" args="-topic robot_description -z 0.28 -P 0.1"/>

    <!-- ROS-Gazebo Bridge -->
    <node pkg="ros_gz_bridge" exec="parameter_bridge" output="screen">
        <param name="config_file" value="$(var gazebo_config_path)"/>
        <param name="use_sim_time" value="true"/>
    </node>

    <!-- Controllers -->
    <node pkg="controller_manager" exec="spawner" output="screen"
          args="joint_state_broadcaster --controller-manager /controller_manager --controller-manager-timeout 30">
        <param name="use_sim_time" value="true"/>
    </node>

    <node pkg="controller_manager" exec="spawner" output="screen"
          args="joint_position_controller --controller-manager /controller_manager --controller-manager-timeout 30">
        <param name="use_sim_time" value="true"/>
    </node>

    <node pkg="controller_manager" exec="spawner" output="screen"
          args="diff_drive_controller --controller-manager /controller_manager --controller-manager-timeout 30">
        <param name="use_sim_time" value="true"/>
    </node>

    <!-- Balance Controller — launched last -->
    <node pkg="bipedal_balance_controller" exec="balance_controller"/>

</launch>